/**
 * @module @conveyor/store-redis
 *
 * Redis-backed store implementation for the Conveyor job queue.
 *
 * **Work in progress** — the full `StoreInterface` implementation lands
 * in follow-up phases. See `tasks/redis-store.md` for the plan.
 */
export {};
